{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C++ in Terminal",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "BuildAndRunFull",
      "console": "integratedTerminal"
    }
  ]
}